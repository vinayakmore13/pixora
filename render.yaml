services:
  # 1. Frontend - React (Static Site)
  - type: static
    name: pixvora-frontend
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_SUPABASE_URL
        sync: false
      - key: VITE_SUPABASE_ANON_KEY
        sync: false
      - key: VITE_BACKEND_URL
        sync: false
      - key: VITE_AI_PROVIDER
        value: LOCAL

  # 2. Backend - FastAPI + DeepFace AI (Web Service)
  - type: web
    name: pixvora-backend
    env: python
    plan: standard  # DeepFace + TensorFlow needs >=1GB RAM
    buildCommand: pip install --upgrade pip && pip install -r backend/requirements.txt
    startCommand: cd backend && gunicorn -k uvicorn.workers.UvicornWorker main:app --bind 0.0.0.0:$PORT --timeout 120 --workers 1
    healthCheckPath: /health
    envVars:
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_SERVICE_ROLE_KEY
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: FRONTEND_URL
        sync: false
      - key: SHARE_SESSION_TTL_SECONDS
        value: "7200"
